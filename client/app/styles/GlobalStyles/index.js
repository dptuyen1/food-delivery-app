import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
    container: {
        flex: 1,
        margin: 20,
    },
    errorText: {
        fontSize: 16,
        fontWeight: "bold",
        color: "red",
    },
});
